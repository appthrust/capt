---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: captcontrolplanetemplates.controlplane.cluster.x-k8s.io
spec:
  group: controlplane.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: CAPTControlPlaneTemplate
    listKind: CAPTControlPlaneTemplateList
    plural: captcontrolplanetemplates
    singular: captcontrolplanetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CAPTControlPlaneTemplate is the Schema for the captcontrolplanetemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CAPTControlPlaneTemplateSpec defines the desired state of
              CAPTControlPlaneTemplate
            properties:
              template:
                description: Template is the template for the CaptControlPlane
                properties:
                  metadata:
                    description: |-
                      Standard object's metadata.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      finalizers:
                        items:
                          type: string
                        type: array
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                  spec:
                    description: |-
                      Spec is the specification for the template. All fields are optional to
                      allow ClusterClass/topology patches to populate values.
                    properties:
                      additionalTags:
                        additionalProperties:
                          type: string
                        type: object
                      controlPlaneConfig:
                        description: ControlPlaneConfigTemplate contains EKS-specific
                          configuration for templates (all optional)
                        properties:
                          addons:
                            items:
                              description: AddonTemplate represents an EKS addon (all
                                optional)
                              properties:
                                configurationValues:
                                  type: string
                                name:
                                  type: string
                                version:
                                  type: string
                              type: object
                            type: array
                          endpointAccess:
                            description: EndpointAccessTemplate defines API server
                              endpoint access (all optional)
                            properties:
                              private:
                                type: boolean
                              public:
                                type: boolean
                              publicCIDRs:
                                items:
                                  type: string
                                type: array
                            type: object
                          region:
                            description: AWS region where the control plane will be
                              created
                            pattern: ^[a-z]{2}-[a-z]+-[0-9]$
                            type: string
                          timeouts:
                            description: TimeoutConfigTemplate defines timeout settings
                              (kept optional)
                            properties:
                              controlPlaneTimeout:
                                default: 30
                                description: ControlPlaneTimeout is the timeout in
                                  minutes for control plane creation
                                minimum: 1
                                type: integer
                              vpcReadyTimeout:
                                default: 15
                                description: VPCReadyTimeout is the timeout in minutes
                                  for VPC ready check
                                minimum: 1
                                type: integer
                            type: object
                        type: object
                      controlPlaneEndpoint:
                        description: APIEndpointTemplate represents the endpoint used
                          to communicate with the control plane (all optional for
                          templates)
                        properties:
                          host:
                            description: The hostname on which the API server is serving.
                            type: string
                          port:
                            description: The port on which the API server is serving.
                            format: int32
                            type: integer
                        type: object
                      version:
                        type: string
                      workspaceTemplateApplyName:
                        type: string
                      workspaceTemplateRef:
                        description: WorkspaceTemplateReferenceTemplate contains the
                          reference to WorkspaceTemplate (all optional for templates)
                        properties:
                          name:
                            description: Name is the name of the WorkspaceTemplate.
                            type: string
                          namespace:
                            description: Namespace is the namespace of the WorkspaceTemplate.
                            type: string
                        type: object
                    type: object
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
