apiVersion: v1
kind: Secret
metadata:
  name: capt-aws-creds
  namespace: upbound-system
type: Opaque
stringData:
  credentials: |
    [default]
    aws_access_key_id = REPLACE_ME
    aws_secret_access_key = REPLACE_ME
    # aws_session_token = REPLACE_ME   # 一時認証を使う場合のみ


