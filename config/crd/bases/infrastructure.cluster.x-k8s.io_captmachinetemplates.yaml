---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: captmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: CAPTMachineTemplate
    listKind: CAPTMachineTemplateList
    plural: captmachinetemplates
    singular: captmachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: CAPTMachineTemplate is the Schema for the captmachinetemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CAPTMachineTemplateSpec defines the desired state of CAPTMachineTemplate
            properties:
              template:
                description: CAPTMachineTemplateResource describes the data needed
                  to create a CAPTMachine from a template
                properties:
                  spec:
                    description: CAPTMachineSpec defines the desired state of CAPTMachine
                    properties:
                      fargateProfile:
                        items:
                          description: FargateProfileConfig defines the desired state
                            of a Fargate profile
                          properties:
                            name:
                              type: string
                            selectors:
                              items:
                                description: SelectorConfig defines the selectors
                                  for a Fargate profile
                                properties:
                                  namespace:
                                    type: string
                                required:
                                - namespace
                                type: object
                              type: array
                          required:
                          - name
                          - selectors
                          type: object
                        type: array
                    required:
                    - fargateProfile
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
          status:
            description: CAPTMachineTemplateStatus defines the observed state of CAPTMachineTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
