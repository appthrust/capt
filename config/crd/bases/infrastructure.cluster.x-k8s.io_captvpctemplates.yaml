---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: captvpctemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: CAPTVPCTemplate
    listKind: CAPTVPCTemplateList
    plural: captvpctemplates
    singular: captvpctemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: CAPTVPCTemplate is the Schema for the captvpctemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CAPTVPCTemplateSpec defines the desired state of CAPTVPCTemplate
            properties:
              cidr:
                description: CIDR is the IP range to use for the VPC
                type: string
              enableNatGateway:
                description: EnableNatGateway determines if NAT Gateway should be
                  created
                type: boolean
              privateSubnetTags:
                additionalProperties:
                  type: string
                description: PrivateSubnetTags are the tags to apply to private subnets
                type: object
              publicSubnetTags:
                additionalProperties:
                  type: string
                description: PublicSubnetTags are the tags to apply to public subnets
                type: object
              singleNatGateway:
                description: SingleNatGateway determines if a single NAT Gateway should
                  be used for all AZs
                type: boolean
              tags:
                additionalProperties:
                  type: string
                description: Tags are the tags to apply to the VPC and all its resources
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToRef specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - cidr
            - enableNatGateway
            - singleNatGateway
            type: object
          status:
            description: CAPTVPCTemplateStatus defines the observed state of CAPTVPCTemplate
            properties:
              vpcId:
                description: VPCID is the ID of the created VPC
                type: string
              vpcStatus:
                description: VPCStatus represents the current status of the VPC
                type: string
              workspaceName:
                description: WorkspaceName is the name of the associated Terraform
                  Workspace
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
